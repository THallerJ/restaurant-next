import HeaderWrapper from "./hocs/HeaderWrapper";
import HeaderInfo from "./components/HeaderInfo";

const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderInfo />
    </HeaderWrapper>
  );
};

export default Header;
